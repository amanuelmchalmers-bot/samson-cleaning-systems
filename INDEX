<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Samson Miljöservice - Professionell Städning i Skaraborg</title>
    <meta name="description" content="Miljövänlig städservice i Skövde och Skaraborg. Flyttstäd, hemstäd och företagstäd med kvalitet i fokus. Boka direkt online!">
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #2ecc71;
            --primary-dark: #27ae60;
            --primary-light: #a8e6c8;
            --accent: #3498db;
            --dark: #2c3e50;
            --gray: #7f8c8d;
            --light-gray: #ecf0f1;
            --white: #ffffff;
            --shadow: rgba(0, 0, 0, 0.1);
            --shadow-lg: rgba(0, 0, 0, 0.15);
        }

        body {
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
            color: var(--dark);
            background: var(--white);
            overflow-x: hidden;
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
            padding: 80px 20px 40px;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg"><circle cx="50" cy="50" r="2" fill="rgba(255,255,255,0.1)"/></svg>');
            animation: float 20s linear infinite;
        }

        @keyframes float {
            0% { transform: translateY(0) translateX(0); }
            100% { transform: translateY(-100px) translateX(100px); }
        }

        .hero-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
            position: relative;
            z-index: 1;
        }

        .hero-text h1 {
            font-size: 3.5rem;
            font-weight: 700;
            color: var(--white);
            margin-bottom: 20px;
            line-height: 1.2;
            animation: slideInLeft 0.8s ease;
        }

        .hero-text h1 .emoji {
            display: inline-block;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .hero-text p {
            font-size: 1.3rem;
            color: rgba(255, 255, 255, 0.95);
            margin-bottom: 30px;
            font-weight: 300;
            animation: slideInLeft 0.8s ease 0.2s both;
        }

        .hero-text .tagline {
            font-size: 1.1rem;
            color: var(--primary-light);
            margin-bottom: 40px;
            font-weight: 400;
            animation: slideInLeft 0.8s ease 0.3s both;
        }

        .cta-group {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
            animation: slideInLeft 0.8s ease 0.4s both;
        }

        .btn {
            padding: 18px 40px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            cursor: pointer;
            border: none;
            box-shadow: 0 4px 15px var(--shadow);
        }

        .btn-primary {
            background: var(--primary);
            color: var(--white);
        }

        .btn-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(46, 204, 113, 0.4);
        }

        .btn-secondary {
            background: var(--white);
            color: var(--dark);
        }

        .btn-secondary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px var(--shadow-lg);
        }

        .hero-visual {
            position: relative;
            animation: slideInRight 0.8s ease;
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .trust-badges {
            display: flex;
            gap: 30px;
            margin-top: 40px;
            flex-wrap: wrap;
        }

        .badge {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            padding: 15px 25px;
            border-radius: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
            color: var(--white);
            font-weight: 500;
        }

        .badge-icon {
            font-size: 1.5rem;
        }

        /* Services Section */
        .services {
            padding: 100px 20px;
            background: var(--white);
        }

        .section-header {
            text-align: center;
            max-width: 800px;
            margin: 0 auto 60px;
        }

        .section-header h2 {
            font-size: 2.8rem;
            color: var(--dark);
            margin-bottom: 20px;
            font-weight: 700;
        }

        .section-header p {
            font-size: 1.2rem;
            color: var(--gray);
            font-weight: 300;
        }

        .service-grid {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
        }

        .service-card {
            background: var(--white);
            border-radius: 20px;
            padding: 40px 30px;
            text-align: center;
            transition: all 0.3s ease;
            border: 2px solid transparent;
            box-shadow: 0 5px 20px var(--shadow);
            position: relative;
            overflow: hidden;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .service-card:hover {
            transform: translateY(-10px);
            border-color: var(--primary);
            box-shadow: 0 15px 40px var(--shadow-lg);
        }

        .service-card:hover::before {
            transform: scaleX(1);
        }

        .service-icon {
            font-size: 3.5rem;
            margin-bottom: 20px;
            display: inline-block;
            animation: float-icon 3s ease-in-out infinite;
        }

        @keyframes float-icon {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .service-card h3 {
            font-size: 1.5rem;
            color: var(--dark);
            margin-bottom: 15px;
            font-weight: 600;
        }

        .service-card p {
            color: var(--gray);
            margin-bottom: 20px;
            font-size: 0.95rem;
        }

        .campaign-badge {
            display: inline-block;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: var(--white);
            padding: 6px 16px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            margin-top: 10px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        /* Why Choose Us */
        .why-choose {
            padding: 100px 20px;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        }

        .features-grid {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
        }

        .feature {
            text-align: center;
        }

        .feature-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            margin: 0 auto 20px;
            box-shadow: 0 10px 30px rgba(46, 204, 113, 0.3);
        }

        .feature h3 {
            font-size: 1.3rem;
            color: var(--dark);
            margin-bottom: 10px;
            font-weight: 600;
        }

        .feature p {
            color: var(--gray);
            font-size: 0.95rem;
        }

        /* Process Section */
        .process {
            padding: 100px 20px;
            background: var(--white);
        }

        .steps {
            max-width: 1000px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 30px;
            flex-wrap: wrap;
        }

        .step {
            flex: 1;
            min-width: 200px;
            text-align: center;
            position: relative;
        }

        .step-number {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            color: var(--white);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0 auto 20px;
            box-shadow: 0 5px 20px rgba(46, 204, 113, 0.3);
        }

        .step h3 {
            font-size: 1.2rem;
            color: var(--dark);
            margin-bottom: 10px;
            font-weight: 600;
        }

        .step p {
            color: var(--gray);
            font-size: 0.9rem;
        }

        /* CTA Section */
        .cta-section {
            padding: 100px 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .cta-section::before {
            content: '🧹';
            position: absolute;
            font-size: 20rem;
            opacity: 0.05;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-15deg);
        }

        .cta-content {
            max-width: 800px;
            margin: 0 auto;
            position: relative;
            z-index: 1;
        }

        .cta-section h2 {
            font-size: 3rem;
            color: var(--white);
            margin-bottom: 20px;
            font-weight: 700;
        }

        .cta-section p {
            font-size: 1.3rem;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 40px;
            font-weight: 300;
        }

        /* Footer */
        .footer {
            background: var(--dark);
            color: var(--white);
            padding: 60px 20px 30px;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-section h3 {
            color: var(--primary);
            margin-bottom: 20px;
            font-size: 1.3rem;
            font-weight: 600;
        }

        .footer-section p,
        .footer-section a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            display: block;
            margin-bottom: 10px;
            transition: color 0.3s;
        }

        .footer-section a:hover {
            color: var(--primary);
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
        }

        .contact-icon {
            color: var(--primary);
            font-size: 1.2rem;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.9rem;
        }

        /* Floating Animation */
        .float-element {
            position: absolute;
            pointer-events: none;
            opacity: 0.1;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-content {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .hero-text h1 {
                font-size: 2.5rem;
            }

            .hero-text p {
                font-size: 1.1rem;
            }

            .cta-group {
                justify-content: center;
            }

            .hero-visual {
                display: none;
            }

            .section-header h2 {
                font-size: 2rem;
            }

            .cta-section h2 {
                font-size: 2rem;
            }

            .steps {
                flex-direction: column;
            }
        }

        /* Smooth Scroll */
        html {
            scroll-behavior: smooth;
        }

        /* Selection Color */
        ::selection {
            background: var(--primary);
            color: var(--white);
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <div class="hero-text">
                <h1>
                    Professionell Städning i Skaraborg <span class="emoji">🧹</span>
                </h1>
                <p>
                    Miljövänlig städservice med kvalitet i fokus. Vi tar hand om ditt hem eller företag med omtanke och expertis.
                </p>
                <div class="tagline">
                    ✨ RUT-avdrag direkt på fakturan • Kostnadsfria offerter • Erfaren personal
                </div>
                
                <div class="cta-group">
                    <a href="#" class="btn btn-primary" onclick="openChat(); return false;">
                        💬 Boka Nu - Få Offert Direkt
                    </a>
                    <a href="#services" class="btn btn-secondary">
                        📋 Se Våra Tjänster
                    </a>
                </div>

                <div class="trust-badges">
                    <div class="badge">
                        <span class="badge-icon">💚</span>
                        <span>Miljövänliga produkter</span>
                    </div>
                    <div class="badge">
                        <span class="badge-icon">⭐</span>
                        <span>100+ nöjda kunder</span>
                    </div>
                    <div class="badge">
                        <span class="badge-icon">🏆</span>
                        <span>Erfaren personal</span>
                    </div>
                </div>
            </div>

            <div class="hero-visual">
                <div style="font-size: 20rem; opacity: 0.2; text-align: center; animation: bounce 3s infinite;">
                    🧹
                </div>
            </div>
        </div>
    </section>

    <!-- Services Section -->
    <section id="services" class="services">
        <div class="section-header">
            <h2>Våra Städtjänster</h2>
            <p>Professionella lösningar anpassade efter dina behov</p>
        </div>

        <div class="service-grid">
            <div class="service-card">
                <div class="service-icon">📦</div>
                <h3>Flyttstädning</h3>
                <p>Professionell städning inför flytt. Vi ser till att allt är perfekt för besiktningen.</p>
                <a href="#" class="btn btn-primary" onclick="openChatWithService('flyttstadning'); return false;">Boka Flyttstäd</a>
            </div>

            <div class="service-card">
                <div class="service-icon">🏡</div>
                <h3>Hemstädning</h3>
                <p>Regelbunden eller engångsstädning av ditt hem. Anpassat efter dina önskemål.</p>
                <a href="#" class="btn btn-primary" onclick="openChatWithService('hemstadning'); return false;">Boka Hemstäd</a>
            </div>

            <div class="service-card">
                <div class="service-icon">✨</div>
                <h3>Storstädning</h3>
                <p>Grundlig städning från golv till tak. Perfekt inför högtider eller säsongsbyten.</p>
                <span class="campaign-badge">🎉 KAMPANJ</span>
                <a href="#" class="btn btn-primary" onclick="openChatWithService('storstadning'); return false;">Boka Nu</a>
            </div>

            <div class="service-card">
                <div class="service-icon">🪟</div>
                <h3>Fönsterputs</h3>
                <p>Kristallklara fönster både in- och utvändigt. Vi når även högt upp.</p>
                <span class="campaign-badge">🎉 KAMPANJ</span>
                <a href="#" class="btn btn-primary" onclick="openChatWithService('fonsterputs'); return false;">Boka Nu</a>
            </div>

            <div class="service-card">
                <div class="service-icon">🏢</div>
                <h3>Företagstäd</h3>
                <p>Städtjänster för kontor och företag. Schemalägg efter era behov.</p>
                <a href="#" class="btn btn-primary" onclick="openChatWithService('foretagstad'); return false;">Kontakta Oss</a>
            </div>

            <div class="service-card">
                <div class="service-icon">🏗️</div>
                <h3>Byggstäd</h3>
                <p>Efterstädning vid renovering och byggnation. Professionellt resultat.</p>
                <a href="#" class="btn btn-primary" onclick="openChatWithService('byggstad'); return false;">Boka Byggstäd</a>
            </div>
        </div>
    </section>

    <!-- Why Choose Us -->
    <section class="why-choose">
        <div class="section-header">
            <h2>Varför Välja Oss?</h2>
            <p>Vi bryr oss om kvalitet, miljön och dina behov</p>
        </div>

        <div class="features-grid">
            <div class="feature">
                <div class="feature-icon">💚</div>
                <h3>Miljövänligt</h3>
                <p>Vi använder endast miljövänliga produkter som är säkra för dig och miljön.</p>
            </div>

            <div class="feature">
                <div class="feature-icon">👥</div>
                <h3>Erfaren Personal</h3>
                <p>Vårt team har många års erfarenhet av både privat- och företagstäd.</p>
            </div>

            <div class="feature">
                <div class="feature-icon">💰</div>
                <h3>RUT-avdrag</h3>
                <p>Vi gör RUT-avdraget direkt på fakturan. Du betalar bara halva priset.</p>
            </div>

            <div class="feature">
                <div class="feature-icon">📅</div>
                <h3>Flexibla Tider</h3>
                <p>Vi anpassar oss efter ditt schema. Boka när det passar dig bäst.</p>
            </div>

            <div class="feature">
                <div class="feature-icon">🎯</div>
                <h3>Kvalitetsgaranti</h3>
                <p>Nöjda kunder är vår huvudprioritet. Vi gör jobbet ordentligt.</p>
            </div>

            <div class="feature">
                <div class="feature-icon">🚀</div>
                <h3>Snabb Bokning</h3>
                <p>Boka online direkt och få offert inom 5 minuter via SMS.</p>
            </div>
        </div>
    </section>

    <!-- Process Section -->
    <section class="process">
        <div class="section-header">
            <h2>Så Här Funkar Det</h2>
            <p>Enkelt, snabbt och smidigt från start till mål</p>
        </div>

        <div class="steps">
            <div class="step">
                <div class="step-number">1</div>
                <h3>Chatta med Oss</h3>
                <p>Klicka på chatknappen och berätta vad du behöver hjälp med.</p>
            </div>

            <div class="step">
                <div class="step-number">2</div>
                <h3>Få Offert</h3>
                <p>Vi skickar en prisoffert via SMS inom 5 minuter.</p>
            </div>

            <div class="step">
                <div class="step-number">3</div>
                <h3>Boka Tid</h3>
                <p>Välj ett datum och tid som passar dig bäst.</p>
            </div>

            <div class="step">
                <div class="step-number">4</div>
                <h3>Vi Städar</h3>
                <p>Vi kommer och gör jobbet med kvalitet och omtanke.</p>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="cta-content">
            <h2>Redo Att Få Ditt Hem Skinande Rent? ✨</h2>
            <p>Boka direkt via vår smarta chatbot och få offert inom 5 minuter!</p>
            <a href="#" class="btn btn-secondary" onclick="openChat(); return false;" style="font-size: 1.2rem; padding: 20px 50px;">
                💬 Starta Chatt & Boka Nu
            </a>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <div class="footer-section">
                <h3>Samson Miljöservice</h3>
                <p>Professionell städservice i Skövde och Skaraborg sedan 2018. Vi bryr oss om kvalitet och miljön.</p>
            </div>

            <div class="footer-section">
                <h3>Kontakt</h3>
                <div class="contact-item">
                    <span class="contact-icon">📞</span>
                    <a href="tel:0762386236">076-238 62 36</a>
                </div>
                <div class="contact-item">
                    <span class="contact-icon">📧</span>
                    <a href="mailto:info@samsonmiljoservice.se">info@samsonmiljoservice.se</a>
                </div>
                <div class="contact-item">
                    <span class="contact-icon">📍</span>
                    <span>Skövde, Skaraborg</span>
                </div>
            </div>

            <div class="footer-section">
                <h3>Tjänster</h3>
                <a href="#services">Flyttstädning</a>
                <a href="#services">Hemstädning</a>
                <a href="#services">Storstädning</a>
                <a href="#services">Fönsterputs</a>
                <a href="#services">Företagstäd</a>
                <a href="#services">Byggstäd</a>
            </div>

            <div class="footer-section">
                <h3>Öppettider</h3>
                <p>Måndag - Fredag: 08:00 - 17:00</p>
                <p>Lördag: 09:00 - 15:00</p>
                <p>Söndag: Stängt</p>
                <p style="margin-top: 15px; color: var(--primary);">Boka online 24/7!</p>
            </div>
        </div>

        <div class="footer-bottom">
            <p>&copy; 2024 Samson Miljöservice AB. Org.nr: 559 411 5312. Alla rättigheter förbehållna.</p>
            <p>💚 Miljöcertifierat • RUT-godkänt • F-skattsedel</p>
        </div>
    </footer>

    <!-- Chat Integration (Load from your chatbot) -->
    <script>
        // Function to open chatbot
        function openChat() {
            const chatWidget = document.querySelector('.chat-widget');
            if (chatWidget) {
                chatWidget.classList.add('active');
            } else {
                alert('Chatboten laddas... Vänligen försök igen om ett ögonblick.');
            }
        }

        // Function to open chat with pre-selected service
        function openChatWithService(service) {
            openChat();
            // This will be implemented to pre-select the service in the chatbot
            setTimeout(() => {
                // Trigger service selection in chatbot
                window.selectedService = service;
            }, 500);
        }

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                const href = this.getAttribute('href');
                if (href !== '#') {
                    e.preventDefault();
                    document.querySelector(href).scrollIntoView({
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Intersection Observer for fade-in animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe all cards and sections
        document.querySelectorAll('.service-card, .feature, .step').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(20px)';
            el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
            observer.observe(el);
        });
    </script>
</body>
</html>
